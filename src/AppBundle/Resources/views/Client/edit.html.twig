{% extends '::base.html.twig' %}

{% block body -%}
<div class="row">
  <div class="col-md-6">
    <h1>Client edit</h1>

    {{ form(edit_form) }}

    <div class="btn-actions">
        <a href="{{ path('clients') }}">
            Back to the list
        </a>
      {{ form(delete_form) }}
    </div>
  </div>
</div>
{% endblock %}

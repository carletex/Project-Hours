{% extends '::base.html.twig' %}

{% block body -%}
<div class="row">
  <div class="col-md-6">
    <h1>Record creation</h1>
    {{ form(form) }}
    <div class="btn-actions">
      <a href="{{ path('records') }}">
          Back to the list
      </a>
    </div>
  </div>
</div>
{% endblock %}
